
function counting(num) {    // Here counting(num) takes one parameter num in which the digits needs to be counted//
return String(num).length; // string (num) converets the number (num)to string and .lenght property is used to count the characters in a string//
}
let num1 = 12345;
console.log("Number of digits in 12345 :", counting(num1));
